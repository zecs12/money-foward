# CLAUDE.md

このファイルは、このリポジトリで Claude Code に作業してもらうときのルールです。
Claude はこのリポジトリで作業する前に、必ずこの内容を確認してください。

## ユーザーについて

- ユーザーはプログラミング・IT の知識がゼロです。
- 「ファイルはどこにありますか」のような質問にも、専門用語(リポジトリ、ブランチ、
  ターミナル、パスなど)は必ず一言説明を添えたうえで、実際に画面のどこを見ればよいか、
  何をクリックすればよいかが分かるレベルまで、丁寧かつ詳細に説明する。
- 省略せず、手順は番号付きの箇条書きなどで順を追って説明する。
- 「前回と同じ手順で」「いつも通り」のように、過去の説明を参照するだけで済ませない。
  ユーザーが既に知っている・覚えていると勝手に仮定せず、毎回、手順の最初から
  すべてを省略せずに書き出す(たとえ以前説明したことがある内容でも同様)。
- 「今開いている画面で」「先ほどの場所で」のように、直前の作業状態が続いている
  ことを前提にした書き方もしない(ターミナルを閉じた・パソコンを再起動した等、
  状態が続いていない可能性を常に考える)。
- 「もし〜が見当たらない場合は、以下の手順で…」のように、フルの手順を
  「見つからなかった場合の予備」として条件分岐の奥に書かない。ウィンドウやアプリを
  開くところから始まる操作は、常に手順の本筋(最初のステップ)として、
  1本の流れで書く。

## 作業の進め方(10のルール)

1. **初心者にも分かる言葉で説明する**
   - ユーザーはプログラミングの知識がゼロという前提で、丁寧かつ詳細に説明する。
   - 専門用語を使うときは、必ず簡単な補足を添える(例:「リポジトリ(コードを保存しておく場所)」)。
   - 「何をしたか」だけでなく「なぜそうしたか」も分かるように説明する。
   - ファイルの場所を聞かれたときは、「〜にあります」で終わらせず、
     実際にどうやってそのファイルを見られるか(GitHub上のリンク、フォルダをたどる手順、
     開き方など)まで、画面操作のレベルで具体的に説明する。

2. **作業前に必ず計画を出す**
   - 実装・変更を始める前に、「何を」「どんな順番で」やるかを箇条書きで提示する。
   - 影響範囲が大きい作業(新しいファイル群の追加、設定変更、認証情報の扱いなど)は、
     着手前にユーザーの確認を取ってから進める。

3. **既存ファイルを勝手に削除しない**
   - 既存のファイル・フォルダを削除する必要がある場合は、理由を説明し、
     必ず確認を取ってから行う。
   - 消してよいか迷う場合は、削除せずに退避(リネーム・移動)する。

4. **不明点は推測せず確認する**
   - 仕様、データ形式、認証情報の扱いなど不明な点があれば、勝手に決めつけずに質問する。
   - 特にお金や個人情報に関わる部分は、必ず確認してから進める。

5. **変更後は変更箇所を報告する**
   - 作業が終わったら「どのファイルの、何を」変更したかを分かりやすくまとめて報告する。
   - 新規作成したファイルも一覧で示す。

6. **命令されてない場所は触らない**
   - 頼まれていないリファクタリングや、関係のないファイルの修正は行わない。
   - 「ついでに直しておいた」はしない。

7. **セキュリティに関わる操作は行わない**
   - パスワードや認証情報をコードに直接書き込まない。`.env` などで管理し、Git には含めない。
   - 個人情報・金融情報を外部（本人が指示していないサービスなど）に送信する実装はしない。
   - 不正アクセスや利用規約に反する可能性のある操作は行わない。

8. **具体例・コードは必要に応じて提示する**
   - 説明だけでなく、実際のコード例やコマンド例を示す。
   - ただし冗長にならないよう、必要な範囲にとどめる。

9. **結論を先に、その後に理由を説明する**
   - 「結論 → 理由」の順で説明する（先に「〜します／〜でした」、その後に「なぜなら〜」）。

10. **最後に、次にやるべきことを提案する**
    - 作業の最後に、次のステップ（次にやると良いこと）を1〜3個程度提案する。

## このプロジェクトについて

マネーフォワードMEから家計データを取得し、自分のパソコンの中だけで見られる
ダッシュボードにまとめて分析・グラフ化するためのツールです。

- **アカウントA**: 楽天銀行（明細）・楽天証券（NISA状況）→ Playwrightで自動ログイン・自動取得
- **アカウントB**: セゾンカード（利用明細）→ 該当月のCSVをダウンロードして取得
- 取得したデータはローカルデータベース（`data/money.db`、SQLite）に保存し、
  Streamlitでパソコンの中だけで見られるダッシュボードとして表示する
  （インターネットには公開しない。ユーザーの希望により、Googleスプレッドシート／Excel出力ではなく
  ローカル完結のWebアプリ方式を採用）

### 認証情報の取り扱い

- ログイン用のメールアドレス・パスワードは `.env` ファイルで管理し、**絶対に Git にコミットしない**。
- 2段階認証（OTP）のコードは自動取得せず、実行時にターミナルで手入力してもらう。
- ブラウザのセッション情報（Cookie等）は `data/` フォルダに保存し、`.gitignore` で除外する。

### 参考にしたツール

- [hiroppy/mf-dashboard](https://github.com/hiroppy/mf-dashboard)（Playwrightでのログイン処理を参考にした。全体構成・Docker常時稼働の仕組みは採用していない）
